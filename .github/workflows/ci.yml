name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build step
        run: |
          echo "Starting build..."
          echo "Build successful!"

      - name: Run tests
        run: |
          echo "No tests configured"
