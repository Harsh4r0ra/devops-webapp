name: CI Pipeline - DevOps WebApp

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run Build Step
        run: echo "Building the DevOps Web Application..."

      - name: Run Test Step
        run: echo "Running tests (no tests configured)"

      - name: Finish Job
        run: echo "CI Pipeline executed successfully!"
